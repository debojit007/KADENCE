[project]
name = "kadence"
version = "0.1.0"
description = "Agent-first AI systems monorepo."
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
  "packages/*",
  "agents/*",
  "apps/*"
]

[tool.ruff]
line-length = 100
target-version = "py312"

