# Resume Agent

`resume-agent` is a KADENCE agent for creating tailored resumes from structured applicant data and a job advert.

The v1 output is a Markdown resume. The workflow must always generate a review artifact before rendering the final resume.

See [SPEC.md](SPEC.md).

