[project]
name = "kadence-resume-reviewer-agent"
version = "0.1.0"
description = "KADENCE final resume artifact reviewer."
requires-python = ">=3.12"
dependencies = []

[project.scripts]
kadence-resume-review = "resume_reviewer_agent.cli:main"
