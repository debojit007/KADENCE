# LLM Provider Adapter

Agents should depend on a small provider interface instead of directly coupling workflow code to a single vendor.

OpenAI is the default provider for KADENCE. Other providers can be added behind the same interface.

