# Audit Logging

Use append-only audit events for consequential workflow steps.

Each event should include timestamp, run id, actor type, event type, artifact references, artifact hashes, provider metadata when relevant, and error details when present.

